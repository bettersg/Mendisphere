rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for local testing/emulator
    // All authenticated users can read and write everything
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
